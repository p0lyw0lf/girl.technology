{
  "scripts": {
    "build": "esbuild index.css --bundle --minify --outdir=dist",
    "watch": "esbuild index.css --bundle --minify --outdir=dist --watch=forever",
    "lint": "stylelint \"**/*.css\""
  },
  "devDependencies": {
    "esbuild": "0.23.1",
    "prettier": "3.3.3",
    "stylelint": "16.8.2",
    "stylelint-config-standard": "36.0.1"
  }
}
